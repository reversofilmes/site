---
layout: null
permalink: /projects.json
---
[
{% assign all_projects = site.data.projects %}
{% if all_projects == nil or all_projects.size == 0 %}
  {% assign all_projects = site.projects %}
{% endif %}
{% for project in all_projects %}
  {% capture search_blob %}{{ project.title | downcase }} {{ project.client | downcase }} {% if project.description %}{{ project.description | strip_html | downcase }} {% endif %}{% if project.service_types %}{{ project.service_types | join: " " | downcase }}{% endif %}{% endcapture %}
  {
    "title": {{ project.title | jsonify }},
    "description": {% if project.description %}{{ project.description | jsonify }}{% else %}null{% endif %},
    "thumbnail": {{ project.thumbnail | jsonify }},
    "hover_preview": {% if project.hover_preview %}{{ project.hover_preview | jsonify }}{% else %}null{% endif %},
    "service_types": {{ project.service_types | jsonify }},
    "client": {{ project.client | jsonify }},
    "date_mmddyyyy": {{ project.date_mmddyyyy | jsonify }},
    "year": {{ project.year }},
    "show_on_home": {{ project.show_on_home | default: false }},
    "order": {% if project.order %}{{ project.order }}{% else %}999{% endif %},
    "home_size": {% if project.home_size %}{{ project.home_size | jsonify }}{% else %}"1x1"{% endif %},
    "pixieset_url": {% if project.pixieset_url %}{{ project.pixieset_url | jsonify }}{% else %}null{% endif %},
    "youtube_url": {% if project.youtube_url %}{{ project.youtube_url | jsonify }}{% else %}null{% endif %},
    "url": {% if project.url %}{{ project.url | jsonify }}{% else %}{{ project.slug | prepend: "/projects/" | append: "/" | jsonify }}{% endif %},
    "search_blob": {{ search_blob | strip | jsonify }}
  }{% unless forloop.last %},{% endunless %}
{% endfor %}
]
